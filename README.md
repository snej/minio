# minio

This is a small C++20 library containing

- Simple but fast stream classes that avoid the bloat of iostreams
- Formatting functions similar to std::format, but which also support printf syntax
- A logging API similar to spdlog but smaller

It's mostly for my own use, but others may find it useful.
